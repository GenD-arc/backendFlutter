services:
  - type: web
    name: backendFlutter
    env: node
    buildCommand: |
      echo "📦 Installing npm packages..."
      npm ci
      echo "🌐 Installing Chrome for Puppeteer..."
      npx puppeteer browsers install chrome
      echo "🔍 Verifying Chrome installation..."
      node -e "console.log('Puppeteer Chrome path:', require('puppeteer').executablePath())"
      echo "✅ Build complete!"
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
